#include "operation.h"
#define SIZE 20

int main (){
    int operation[SIZE];
    uint32_t bit1[SIZE], bit2[SIZE];
    bit1[1] = 0xa7;
    bit2[1] = 0xa7;
    // XOR(bit1[1], bit2[1]);
    NOT(bit1[1]);

    return 0;
}